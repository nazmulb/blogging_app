{{ form_start(form, { attr: {'class': 'form admin-post-form'}}) }}
    <div class="form-group">
        {{ form_row(form.title, { attr: {'class': 'form-control admin-post-form-input'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.content, { attr: {'class': 'form-control admin-post-form-input'}}) }}
    </div>
    <button class="btn btn-secondary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
